/**
 * 配置目录的模板 —— **人工维护；改动前请先确认它是不是输出契约**。
 *
 * 这个字符串不是随便写的说明文本，它参与输出契约：
 *   - `init` 把它写进 `~/.splunk-cli/config.env`，`config` 报告该文件是否存在；
 *   - 文本与 JSON 输出都依赖它逐字节稳定（用户与脚本会 diff 这个文件）。
 *
 * 它曾由脚本生成以避免手抄漂移；生成器已退役，契约改由测试与评审守住：
 * 改动模板时必须同时确认 `init` / `config` 的文本与 JSON 输出仍然成立，并对照
 * 实机验收记录的观察结果。
 */

/** 首次运行时写入的 `config.env` 模板。 */
export const CONFIG_TEMPLATE = "# Splunk CLI configuration\n# ------------------------\n# Generated by `splunk-cli`. Fill in the connection settings below and save.\n#\n# Endpoint: set SPLUNK_HOST and SPLUNK_PORT. No URL needs to be written, and the\n# host is deliberately left blank so nothing about a specific environment is baked\n# into this template.\n#\n# Precedence: shell environment variables override this file, which overrides a\n# local ./.env file, which overrides the built-in defaults.\n#\n# Verify with:  splunk-cli config      (shows `password  <set>`, never the value)\n#               splunk-cli health      (proves connectivity, TLS and credentials)\n\n# Splunk host or IP address, without a scheme and without a port.\n# Examples:  splunk.internal     10.0.0.5\nSPLUNK_HOST=\n\n# REST API management port. This is normally 8089.\n# NOTE: 8000 is the Splunk Web UI port -- the REST API is NOT on 8000.\nSPLUNK_PORT=8089\n\n# Splunk account name. Examples:  admin     ci_reader (a dedicated read-only account is recommended)\nSPLUNK_USERNAME=\nSPLUNK_PASSWORD=\n\n# Optional: use http:// instead of https://. Development only.\n# SPLUNK_INSECURE=false\n\n# Optional: a complete base URL, which overrides SPLUNK_HOST/SPLUNK_PORT when set.\n# Use this for a reverse proxy or a path-prefixed deployment, e.g.\n#   SPLUNK_URL=https://splunk.example.com/services-collector\n# SPLUNK_URL=\n\n# TLS verification. ON by default: the client proves the server is who it claims\n# to be. Splunk's own out-of-the-box certificate (SplunkServerDefaultCert) is\n# signed by Splunk's own CA and carries no SAN, so verification FAILS against a\n# default install -- point SPLUNK_CA_BUNDLE at that CA, or set this to `false`\n# (development only). The connection is encrypted either way; `false` only skips\n# proving the server's identity.\nSPLUNK_VERIFY_SSL=true\n\n# CA bundle (PEM). Needed whenever SPLUNK_VERIFY_SSL=true and the server\n# certificate is not signed by a public CA. Generate it from the WHOLE chain the\n# server sends -- the leaf alone does NOT establish trust:\n#   openssl s_client -connect <host>:8089 -showcerts </dev/null 2>/dev/null | sed -n '/BEGIN CERTIFICATE/,/END CERTIFICATE/p' > splunk-ca.pem\n# The certificate must also carry a SAN matching the host you connect to --\n# connecting by IP requires an iPAddress SAN, because a CN is never matched\n# against an IP address.\n# SPLUNK_CA_BUNDLE=/path/to/splunk-ca.pem\n\n# Per-request HTTP timeout in seconds.\nSPLUNK_TIMEOUT=30\n\n# Safety ceilings. Requests that exceed them are rejected, never silently adjusted.\nSPLUNK_MAX_RESULTS=5000\nSPLUNK_MAX_TIME_RANGE=7d\nSPLUNK_MAX_QUERY_LENGTH=10000\n\n# Search-job budget and polling cadence. Not ceilings; these decide how long a job may\n# run and how often its status is polled. The effective budget is\n# max(SPLUNK_SEARCH_TIMEOUT, SPLUNK_TIMEOUT), so lowering only the former may not help.\nSPLUNK_SEARCH_TIMEOUT=60\nSPLUNK_POLL_INTERVAL=1\n\n# Transport-level retries (never applied to authentication failures or 4xx).\nSPLUNK_MAX_RETRIES=3\nSPLUNK_RETRY_BACKOFF=0.5\n\n# Honour proxy environment variables and the OS proxy configuration.\n# Keep `false` for an internal Splunk: routing its management API through a\n# developer's system proxy produces confusing gateway errors.\nSPLUNK_TRUST_ENV=false\n"
